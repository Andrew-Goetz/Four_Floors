#include "constants.h"
#include "defs.h"

#define SAVEFILE ".four_floors_save" //TODO might want to do some base64 schenanigans to keep user from changing it
/* Initializes a default savefile with default character parameters*/
int init_save(char *name) {
	return 0;
}

//@TODO: find out what needs to go here, may need to rework newCharacter
//int load_save(void) {
//
//}

/* Writes current character information and level progress to savefile */
int save(void) {
	return 0;
}
